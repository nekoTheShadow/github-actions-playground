name: hugo

on: [push]

jobs:
  hugo:
    runs-on: ubuntu-latest
    steps:
    - name: install-hugo
      run: sudo snap install hugo
    - name: check
      run: |
        which hugo
        hugo version
