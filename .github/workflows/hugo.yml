name: hugo

on: [push]

jobs:
  hugo:
    runs-on: ubuntu-latest
    steps:
    - name: install-hugo
      run: |
        mkdir $HOME/src
        cd $HOME/src
        git clone https://github.com/gohugoio/hugo.git
        cd hugo
        go install --tags extended
